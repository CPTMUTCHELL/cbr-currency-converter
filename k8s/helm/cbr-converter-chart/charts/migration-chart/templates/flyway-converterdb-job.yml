apiVersion: batch/v1
kind: Job
metadata:
  name: flyway-converterdb-job
spec:
  template:
    metadata:
      labels:
          app: {{ .Values.name }}
    spec:
      restartPolicy: {{ .Values.restart }}
      containers:
        - name: {{ .Values.name }}-converterdb
          image: cptmutchell/{{ .Values.name }}-converterdb:v1
          command: ["flyway", "migrate"]
