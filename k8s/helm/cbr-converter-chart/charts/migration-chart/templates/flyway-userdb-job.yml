apiVersion: batch/v1
kind: Job
metadata:
  name: flyway-userdb-job
spec:
  template:
    metadata:
      labels:
          app: {{ .Values.name }}
    spec:
      restartPolicy: OnFailure
      containers:
        - name: {{ .Values.name }}-userdb
          image: cptmutchell/{{ .Values.name }}-userdb:v1
          command: ["flyway", "migrate"]
