apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: jenkins-role-binding
  namespace: "{{ns}}"
subjects:
  - kind: User
    name: "{{userCN}}"
    apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: Role
  name: jenkins-role
  apiGroup: rbac.authorization.k8s.io